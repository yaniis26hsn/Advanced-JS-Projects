let request = new XMLHttpRequest() ;
request.open("GET","rabbit.com");
request.responseType() = "json" ;
request.send() ;
request.onload = function(){
    if(request.status < 300 && request.status >= 200){
       let posts = this.response ; // or no need to that this. ? 
    let i = 1 ;
    
    for(post of posts){
        // you can do with them what we want to , for example : 
        console.log("post " i + "is : " + post) ;
        alert("successful req") 
    }  
    }else{

        alert("error") ;
    }
   
} 
